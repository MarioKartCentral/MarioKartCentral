# API
Example bodies for the /api/minio endpoints:

GET: {
    "bucket":"test",
    "file":"file1.txt"
}

POST: {
    "bucket":"test",
    "file":"file1.txt",
    "message":"Hello, MinIO! I am content in a MinIO object."
}